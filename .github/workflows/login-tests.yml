name: Login Tests CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test-login:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      
      - name: Install frontend dependencies
        run: |
          cd frontend
          npm install
          
      - name: Run Login Unit Tests with Vitest
        run: |
          cd frontend
          npx vitest \
            src/pages/Login.test.jsx \
            src/utils/validateLogin/validateTest.test.js \
            --reporter=junit \
            --outputFile=test-results/report.xml
        
      - name: Archive Allure Test Data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-raw-data
          path: frontend/allure-results
          retention-days: 1

      - name: Upload Allure Report Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-html-report
          path: allure-report
          retention-days: 1
